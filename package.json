{
  "name": "pipas-store-brasil",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs && node tests/database.cjs && node tests/requests-database.cjs",
    "build": "node scripts/build.cjs",
    "preview": "node scripts/serve.cjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
